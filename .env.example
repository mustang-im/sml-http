# ---- Server ----
PORT=3000
PUBLIC_BASE_URL=http://localhost:3000

# ---- JWT ----
JWT_CONFIRM_SECRET=changeme_confirm
JWT_ACCESS_SECRET=changeme_access
JWT_CONFIRM_TTL=15m
JWT_ACCESS_TTL=7d

# ---- Database (used by the Node app) ----
DB_HOST=db
DB_PORT=3306
DB_NAME=sml
DB_USER=sml
DB_PASSWORD=sml

# ---- Database (used by docker compose / mysql container) ----
DB_ROOT_PASSWORD=root

# ---- SMTP ----
MAIL_FROM=no-reply@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=user@example.com
SMTP_PASS=password
